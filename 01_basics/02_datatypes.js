"use strict" //treat all js code as newer version
//alert(3+3) //we r using nodejs not browser so it will not run
console.log(3
    +3) //code readibility should be high
console.log("sanya")

let name = "sanya"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => value not assigned
// symbol => unique


// object

console.log(typeof undefined); //returns undefined [undefined itself is a type in js]
console.log(typeof null); // returns object[object is a type of null]